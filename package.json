{
  "name": "blog",
  "version": "1.0.0",
  "author": "reco_luan",
  "scripts": {
    "dev": "vuepress dev . --open --host \"localhost\"",
    "build": "vuepress build .",
    "deploy": "gh-pages -d dist",
    "deploy:build": "yarn run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.2",
    "vue-demi": "^0.13.6",
    "vuepress": "1.8.2",
    "vuepress-theme-reco": "1.5.7"
  },
  "description": "",
  "license": "MIT"
}
